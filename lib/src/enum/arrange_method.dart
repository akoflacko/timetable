/// Enum for the arrange method of the recurring events in the timetable.
///
/// - [ArrangeMethod.stacking]: Events are stacked on top of each other.
/// - [ArrangeMethod.parallel]: Events are placed parallel to each other.
enum ArrangeMethod {
  stacking,
  parallel,
}
